{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:aSite": "lerna --scope @mono-repo-demo/admin-site run start",
    "start:cSite": "lerna --scope @mono-repo-demo/customer-site run start"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
